import React from 'react';
import styled from 'styled-components';

const CheckBoxBlock = styled.input``;

const CheckBox = props => <CheckBoxBlock type="checkbox" {...props} />;

export default CheckBox;
